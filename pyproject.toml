[project]
name = "visir-api"
version = "0.2.0"
description = "ERP inteligente VISIR — API · RAG Fiscal · ChromaDB · Supabase"
readme = "README.md"
requires-python = ">=3.12, < 3.13"

dependencies = [
    # ── HTTP y FastAPI ────────────────────────────────────────────────────────
    "email-validator>=2.3.0",
    "fastapi>=0.136.1",
    "pydantic>=2.13.4",
    "pydantic-settings>=2.14.1",
    "python-multipart>=0.0.29",
    "supabase>=2.30.0",
    "uvicorn>=0.47.0",
    # ── Extracción de CFDIs — OCR + LLM (módulo Extraccion/) ─────────────────
    "numpy==1.26.4",
    "paddlepaddle==3.2.1",
    "paddleocr==3.5.0",
    "PyMuPDF==1.27.2.3",
    "opencv-python==4.11.0.86",
    "Pillow==11.1.0",
    "mistralai==2.4.5",
    "lxml>=6.1.1",
    # ── Async / observabilidad (V13) ─────────────────────────────────────────
    "celery>=5.6.3",
    "redis>=5.3.1",
    "structlog>=26.1.0",
    "sentry-sdk>=2.64.0",
    # ── ChromaDB — vector store ───────────────────────────────────────────────
    "chromadb>=0.5.0",
    # ── Fix: protobuf 5.x/6.x rompe chromadb/opentelemetry en runtime ────────
    "protobuf>=3.20.0,<5.0.0",
    # ── Embeddings (cliente HTTP OpenAI-compatible) ───────────────────────────
    "requests>=2.31.0",
    # ── LlamaIndex — chunking semántico e interfaz BaseEmbedding ─────────────
    "llama-index-core>=0.10.0",
    "pymupdf4llm>=0.0.17",
    "tqdm>=4.66.0",
    # ── LangChain / LangGraph — cadena RAG + máquina de estados con LLM ──────
    "langchain>=0.3.0",
    "langchain-core>=0.3.0",
    "langchain-openai>=0.2.0",
    "langchain-text-splitters>=0.2.0,<1.0",
    "langgraph>=0.2.0",
    # ── MLflow / DagsHub — consumo del modelo de confianza @produccion ────
    "mlflow>=2.20.0",
    "dagshub>=0.3.0",
    # pandas (compatible con numpy==1.26 necesario por paddlepaddle)
    "pandas>=2.0,<2.3",
    # ── Utilidades ────────────────────────────────────────────────────────────
    "python-dotenv>=1.0.0",
    "sentence-transformers>=5.6.1",
]

[dependency-groups]                 # Dependencias de desarrollo
dev = [                             # Desarrollo, testing, calidad
    "httpx>=0.28.1",                # Cliente http moderno
    "mypy>=2.1.0",                  # Revisa los tipos
    "pre-commit>=4.6.0",            # Automatiza las revisiones antes de subir el commit
    "pytest>=9.0.3",                # Testing
    "ruff>=0.15.13",                # ordenamiento
    "types-redis>=4.6.0.20241004",
]

[tool.ruff]
target-version = "py312"
line-length = 100

[tool.uv]
environments = [
    "sys_platform == 'win32'",
    "sys_platform == 'linux'",
]

[tool.ruff.lint]
select = [
    "E", "W", "F", "I", "N", "UP", "B", "C4", "SIM", "ASYNC", "RUF",
]
ignore = [
    "B008",
]

[tool.ruff.lint.per-file-ignores]
"tests/*" = ["S101"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"

[tool.mypy]
python_version = "3.12"
ignore_missing_imports = true
plugins = ["pydantic.mypy"]
disallow_untyped_defs = true
warn_return_any = true
warn_unused_ignores = true

[[tool.mypy.overrides]]
module = "tests.*"
disallow_untyped_defs = false

[tool.pydantic-mypy]
init_typed = true

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.hatch.build.targets.wheel]
packages = ["app", "rag", "ingestion"]
